spring:
  application:
    name: science-news-api
  datasource:
    url: jdbc:postgresql://localhost:5432/science_news_db
    username: omar.mali
    password: Jsparoooo2001##
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080